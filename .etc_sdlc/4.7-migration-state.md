# 4.7 Migration State

Source of truth for migration progress. Update after every phase.
Read this first on any resume.

Referenced spec: `/Users/jason/src/etc-system-engineering/opus-4.7-migration-spec.md`

**Branch:** `4.7-migration` (branched off `main` at commit `2a08c30`).
On rollback, `git checkout main && git branch -D 4.7-migration` restores
the pre-migration baseline. On success, merge back to main.

---

## Phase 0: Snapshot & baseline — **COMPLETE**

**Baseline commit:** `91483b7` — `chore: pre-migration cleanup + v1.8 and 4.7-migration specs`
**Baseline date:** 2026-04-17

**Test count (baseline):** 409 passing, 0 failing
**Compile counts (baseline):**
- gates: 17
- hooks: 14
- agents: 21 (registered in YAML; 24 .md files on disk)
- skills: 10
- standards: 38

**Prompt surface inventory:**
- agents/ .md files (excluding .meta/): 24
- skills/SKILL.md files: 10
- Agent/prompt-type hooks in spec/etc_sdlc.yaml: 3
- standards/ .md files (excluding .meta/): 31

**AP baseline counts (see `.etc_sdlc/4.7-audit/before.txt`):**

Phase 1 expanded the AP-001 grep pattern after catching "idiomatic" and
"strict typing" and "proper" as vague quality descriptors in
`agents/backend-developer.md`. Baseline was re-captured with the
expanded pattern; AP-001 count grew 14 → 20 (6 new matches surfaced).
See spec section 3 AP-001 for the revised pattern.

| AP | Count | Notes |
|----|-------|-------|
| AP-001 | 20 | vague quality descriptors (expanded pattern) |
| AP-002 | 0 | be thorough / comprehensive — already clean |
| AP-003 | 17 | edge case / corner case / handle cases |
| AP-004 | 5 | where applicable / as needed |
| AP-005 | 7 | progress scaffolding |
| AP-006 | 8 | implicit subagent dispatch (skills + yaml) |
| AP-007 | 3 | implicit tool use (narrow patterns) |
| AP-008 | N/A | manual audit; no grep possible |
| AP-009 | 3 | tone assumptions |
| AP-010 | 64 | example-as-scope — LARGEST CATEGORY |
| AP-011 | 1 | implicit DoD (skills only) |
| AP-012 | 26 | cyber-adjacent phrasing — expected high in security-reviewer |
| **TOTAL** | **154** | before migration audit (revised 148→154 after expansion) |

**Phase 0 verification (per spec section 5.7):**
- [x] `git status` shows clean tree
- [x] `.etc_sdlc/4.7-audit/before.txt` exists and is non-empty
- [x] `.etc_sdlc/4.7-migration-state.md` exists (this file)
- [x] Test count recorded (409)

**Observations worth carrying forward:**

1. **AP-012 is expected high.** The security-reviewer agent is OWASP-
   trained; "vulnerability," "exploit," "attack" are contextually
   appropriate. Phase 4 (hook/standards audit) needs to apply the
   AP-012 fix (explicit context framing) without deleting the domain
   terms. Most of the 26 matches are probably load-bearing.

2. **AP-010 at 64 is the biggest category.** "for example / e.g. /
   such as" is heavily used. Some matches are safe illustrations (a
   known-exhaustive list prefixed with "e.g."), others are scope
   conflation risks. Per-file judgment required; no mechanical fix.

3. **AP-002 is already zero.** "Be thorough" and variants don't appear.
   The etc harness was already explicit about this. Good sign.

4. **24 agent .md files but only 21 registered.** Per earlier session
   discussion: `spec-enforcer` was registered in v1.7; `frontend-dashboard-
   refactorer`, `gemini-analyzer`, `multi-tenant-auditor` remain
   unregistered. Migration audits all 24 (the files exist; they may be
   registered later). The compile count of 21 refers only to registered.

---

## Phase 1: Prototype on `agents/backend-developer.md` — **COMPLETE, OPERATOR APPROVED**

**Pre-edit brief:** `.etc_sdlc/4.7-audit/backend-developer-brief.md`
**Edits applied:** 8 total
  - Frontmatter description (AP-001)
  - Body opening line 30 (AP-001, caught after pattern expansion)
  - Responsibilities #2 (AP-001)
  - Before Starting fallback location (AP-004-ish)
  - Python Antipatterns #3 "proper caching" (AP-001, caught by expanded grep)
  - Python Antipatterns #4 (AP-004)
  - Output Format verbosity directive (AP-008, new paragraph added)
  - Error Recovery "Python best practices" fallback (AP-001)

**Spec revisions during this phase:**
- AP-001 grep pattern expanded to include `idiomatic`, `strict typing`,
  `proper`, `robust`, `well-designed`. Per spec section 11.2 Trap 1,
  this was added to the catalog before fixing new instances.
- Baseline `.etc_sdlc/4.7-audit/before.txt` re-captured with the
  expanded pattern (total rose 148 → 154).
- **AP-013 added to the catalog** (reference without read-enforcement).
  Formalizes a structural check that was verified implicitly for
  backend-developer.md and must be applied to every Phase 2 agent.

**Test results:**
- Test A (mechanical AP grep on `agents/backend-developer.md`): **PASS**
  All 9 greppable AP patterns report zero matches.
- Test B (behavioral — two subagent experiments): **PASS**
  - Experiment 1 (knowledge task, no code): agent read 3 of 7 forced-
    read files. Files reported transparently via Files Not Available.
    Quoted all three standards verbatim including the v1.7 enforcement
    annotations — proof of actual read.
  - Experiment 2 (TDD code task): agent read **all 7** forced-read
    files. Wrote 8 passing tests and the implementation. Reported TDD
    hook blocks transparently, used heredoc workaround within authorized
    latitude. Verbosity directive held.
- Test C (full regression suite): **PASS** — 409/409 passing, matches
  Phase 0 baseline.
- AP-013 structural check on `backend-developer.md`: **PASS**. All
  standards references are on the Before Starting mandatory-reads list.

**Key empirical finding (Experiment 2):** The "before writing any code"
conditional in the Before Starting section works under 4.7 literalism.
Non-code tasks trigger partial reads (efficient + transparent);
code-writing tasks trigger full reads. The chicken-and-egg concern
raised during Phase 1 is resolved. AP-013 still codified as a discipline
check, but the existing pattern is 4.7-safe.

**Operator approval:** granted 2026-04-17.

---

## Phase 2: Apply to remaining agents — IN PROGRESS

**Batch 1 (complete, commit `c2ba075`):**
- technical-writer.md — 2 edits, AP-013 PASS
- devops-engineer.md — 4 edits, AP-013 PASS
- code-simplifier.md — 8 edits (added missing Before Starting), AP-013 PASS
- frontend-developer.md — 2 edits, AP-013 PASS
- ux-designer.md — 4 edits, AP-013 PASS

Harness-wide AP deltas: AP-001 −4, AP-003 −5, AP-004 −1, AP-010 −5.
Tests: 409/409 green. Compile: unchanged.

**Batch 2 (complete, commit `b5b39e4`):**
- code-reviewer, architect-reviewer, security-reviewer, verifier, spec-enforcer
- Notable: code-reviewer had three non-existent standards-file refs (latent AP-013 bug fixed)
**Batch 3 (complete, commit `171bdf1`):**
- architect, product-manager, product-owner, ui-designer, domain-modeler
- Notable: architect has "proceed with best judgment" that escaped AP-001 grep — tracked as B1 in bugs-surfaced.md
**Batch 4 (complete, commit `af32757`):**
- researcher, multi-tenant-auditor, process-evaluator, frontend-dashboard-refactorer, gemini-analyzer
- Notable: multi-tenant-auditor and frontend-dashboard-refactorer had no Before Starting section (latent AP-013 bugs fixed)
**Batch 5 (complete, commit `29ccb58`):** sem, hotfix-responder, project-bootstrapper
- sem: 7 edits, invariants C1/C2/C3 preserved, new Subagent Dispatch section
- hotfix-responder: 4 edits, "bypass" preserved with defensive framing
- project-bootstrapper: 10 edits, brownfield/greenfield modes preserved

## Phase 2 — COMPLETE

- 23 agents audited (all except the already-done backend-developer)
- 5 batches, ~40-50 minutes of wall-clock time
- ~90 edits across all agents
- 0 regressions (409/409 tests green throughout)
- Compile counts unchanged (17 gates, 14 hooks, 21 agents, 10 skills, 38 standards)
- 5 latent bugs surfaced and fixed in-flight (Category A in bugs-surfaced.md)

**Residual AP matches across agents/ are all legitimate:**
- 21 × AP-012 in defensive-framed files (security/hotfix/multi-tenant/architect-reviewer/code-reviewer)
- 1 × AP-001 and 1 × AP-010 in `.meta/description.md` (non-directive inventory file, out of scope)

**Follow-up bugs tracked:** `.etc_sdlc/4.7-audit/bugs-surfaced.md`
  - Category A: fixed in-flight (7 items across this session)
  - Category B: tracked for post-migration (6 items, ~9-10 hours)
  - Category C: open questions (3 items)

---

## Phase 3: Audit skills — **COMPLETE**

All 10 skills audited sequentially per spec section 8 (skills
cross-reference; sequential prevents conflicts).

| Skill | Edits | Pattern | Commit |
|---|---|---|---|
| build | 8 | Non-Negotiable orchestrator | `3e7c433` |
| decompose | 5 | Non-Applicable engine | `3e7c433` |
| implement | 6 | Non-Negotiable + QUICK carve-out | `3e7c433` |
| spec | 7 | Non-Applicable Socratic | `3e7c433` |
| hotfix | 7 (rewrite) | Non-Negotiable single-dispatch | `3e7c433` |
| checkpoint | 1 (rewrite) | Non-Applicable state op | `3e7c433` |
| init-project | 7 | Non-Negotiable scoped to project-bootstrapper | `3e7c433` |
| postmortem | 1 (rewrite) + 2 AP-010 | Non-Applicable | `3e7c433` |
| pull-tickets | 10 | Non-Applicable pipeline | `0b1ee27` |
| tasks | 1 (rewrite) + 1 AP-010 | Non-Applicable CLI wrapper | `0b1ee27` |

**Harness-wide residuals (all legitimate exclusions per spec section 12):**
- 11 × various AP matches in `skills/init-project/templates/` (verbatim-
  copy templates for consumer projects, non-directive)
- 3 × AP-012 in `/hotfix` (defensive-framed for "bypass")

**Test fix during Phase 3:** `tests/test_init_project.py` — changed
`find()` to `rfind()` for the placeholder-before-invocation check.
The new Subagent Dispatch policy section mentions `subagent_type` early;
the actual invocation in Phase 1 Step 2 still comes after placeholder
creation. Test was not sophisticated enough to distinguish.

**Bug B7 added to bugs-surfaced.md:** /implement vs /build contract
divergence (5 specific gaps identified; operator decision needed).

---

## Phase 4: Audit hooks and directive standards — not started

---

## Phase 5: Regression test and ship — not started
