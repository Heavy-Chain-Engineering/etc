task_id: "{NNN}"
title: "{Clear, actionable title}"
assigned_agent: backend-developer
status: pending
parent_task: null              # Set to parent ID for subtasks (e.g., "002")
complexity: null               # Auto-scored 1-10 by tasks.py score
wave: null                     # Execution wave (0=first, parallel within wave)
requires_reading:
  - spec/{prd}.md
files_in_scope:
  - src/{module}.py
  - tests/test_{module}.py
acceptance_criteria:
  - "{Specific, measurable criterion}"
dependencies: []               # Task IDs that must complete first
context: |
  {Additional context from the PRD or parent task.}
